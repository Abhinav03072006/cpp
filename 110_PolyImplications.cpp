#include <iostream>
using namespace std;

int main() {
    cout << "Implications of Polymorphism:" << endl;
    cout << "1. Code Reusability (Write once, handle many types)" << endl;
    cout << "2. Flexibility (New derived classes don't break old code)" << endl;
    cout << "3. Overhead (vTable lookup takes slight runtime cost)" << endl;
    return 0;
}